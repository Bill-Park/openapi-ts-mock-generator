name: Version and Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version type to bump (major, minor, patch)"
        required: true
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  version_and_publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # specify your node version
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm install

      - name: Build package
        run: npm run build

      - name: Set git config
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"

      - name: Bump version
        id: bump_version
        run: npm version ${{ github.event.inputs.version }}

      - name: make Pull Request
        if: success()
        run: |
          git checkout -b release-${{ steps.bump_version.outputs.version }}
          git push --set-upstream origin release-${{ steps.bump_version.outputs.version }}
          gh pr create --title "Release ${{ steps.bump_version.outputs.version }}" --body "Release ${{ steps.bump_version.outputs.version }}" --base main --head release-${{ steps.bump_version.outputs.version }} --label "version bump"
